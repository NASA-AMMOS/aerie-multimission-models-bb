# BSP files should be stored via git LFS
*.bsp filter=lfs diff=lfs merge=lfs -text

# Linux start script should use lf
/gradlew text eol=lf

# These are Windows script files and should use crlf
*.bat text eol=crlf
